[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "rlm-dag"
version = "0.1.0"
description = "DAG orchestration protocol as a callable kernel skill (LAYERS.md §4.1): layer-batched llm.query dispatch, cheapest deterministic verification, seed retry, dict assembly"

[tool.setuptools]
py-modules = ["rlm_dag"]